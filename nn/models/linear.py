from ..model import Model
def LinearModel(input_dim, output_dim):
    """
    Single-layer linear model (no activation).

    Useful for debugging and testing.
    """
    layers = []

    # TODO: just one Dense layer

    return Model(layers)